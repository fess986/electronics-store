block variables
  - var pathContentImg = 'img/'
  - var pathIconImg = 'img/icons/'

doctype html
html(lang='ru-RU')
  head
    meta(charset="UTF-8")
    meta(name="viewport", content="width=device-width, initial-scale=1.0")
    //- добавление шрифтов в прелоадер для ускорения загрузки. не всегда есть смысл, так как они и так будут быстро подключаться. Болд-версию прелоадить еще меньше смысла.
    link(rel="preload" href="fonts/Inter-Bold.woff2" as="font" crossorigin="anonymous")
    link(rel="preload" href="fonts/Inter-Medium.woff2" as="font" crossorigin="anonymous")
    link(rel="preload" href="fonts/Inter-Regular.woff2" as="font" crossorigin="anonymous")
    link(rel="preload" href="fonts/Inter-Regular.woff2" as="font" crossorigin="anonymous")

    //- добавление стилей в прелоадер для ускорения загрузки. С другой стороны это не сильно отличается от обычного стайлшита, т.к. приоритет и там и там высокий. прелоадить есть смысл стили, которые могут использоваться на других страницах
    link(rel="preload" href="css/style.css" as="style")
    meta(http-equiv="X-UA-Compatible", content="ie=edge")
    link(rel="stylesheet" type="text/css" href="./css/style.css")
    title #{title}
  body.page
    include ../modules/header
    block content
    include ../modules/footer
